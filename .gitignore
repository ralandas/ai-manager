# dependencies
node_modules/

# build output
dist/

# secrets / env
.env
.env.*
!.env.example

# telegram account pool + session state (SECRET — never commit)
data/pool.json
data/*-state.json
data/*.session

# runtime state (guest contacts) — not content
data/booking-contacts.json

# runtime state (per-chat conversation history + session) — contains chat text
data/conversations.json

# logs
*.log
logs/

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# uploaded apartment photos (runtime)
data/photos/

# Private guest chat screenshots — never commit (real PII + owner payment data)
photos/


# Bnovo reverse-engineering dumps — live session cookie + guest PII, never commit
bookings.json
logs_network.txt
bnovo_*.json
logs2_metwork.txt

# Bnovo raw reverse notes contain login/password — keep out of git
knowledge/_bnovo_reverse.md
