# dependencies
node_modules/

# build output
dist/

# secrets / env
.env
.env.*
!.env.example

# telegram account pool + session state (SECRET — never commit)
data/pool.json
data/*-state.json
data/*.session

# runtime state (guest contacts) — not content
data/booking-contacts.json

# logs
*.log
logs/

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# uploaded apartment photos (runtime)
data/photos/

# Private guest chat screenshots — never commit (real PII + owner payment data)
photos/

# Owner business knowledge distilled from private chats (card number, box codes, wifi)
knowledge/
